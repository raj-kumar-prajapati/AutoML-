import '../../src/index.css'

export const metadata = {
  title: 'Dashboard',
  description: 'The ModelForge AutoML dashboard.',
}

export default function DashboardLayout({ children }) {
  return children
}
